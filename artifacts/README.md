# artifacts

Committed outputs that are referenced for the record but not produced by the current
pipelines (current experiment outputs are git-ignored under `experiments/*/results*`
and `examples/helm/results*`).

- `synthetic-report/` -- the original "Double-Kernel DKPS" synthetic study: report.md
  with its CSVs and figures (superseded by `experiments/unpaired/` and the paper's
  Figure 2).
- `figures/` -- one-off analysis figures (EEE datastore paired-fraction).
